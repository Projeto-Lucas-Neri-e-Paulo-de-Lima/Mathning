/** Raio e tokens estáticos; cores dinâmicas via useTheme(). */
export { radius } from "./radius";
export type { ColorTokens, ResolvedThemeMode, ThemeMode } from "./tokens";
export { getThemeColors } from "./palettes";
